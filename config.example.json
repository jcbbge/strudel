{
  "pantry": {
    "roots": [
      "~/.pi/agent",
      "~/.claude",
      "~/.config/opencode"
    ]
  },
  "embeddings": {
    "_comment": "Optional (L1). Omit this whole block to use zero-infra lexical search. OpenAI-compatible: works with a local server, a hosted endpoint, or any provider via apiKey.",
    "baseUrl": "http://127.0.0.1:10240/v1",
    "model": "mlx-community/Qwen3-Embedding-4B-4bit-DWQ",
    "apiKey": ""
  }
}
