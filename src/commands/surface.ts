/**
 * /strudel surface — show surface control state
 *
 * Shows what's active, what's baseline, what got suppressed.
 */

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function registerSurfaceCommand(_pi: ExtensionAPI): void {
	// TODO: Implement surface command
}
