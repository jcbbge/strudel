/**
 * /strudel pantry — list the full primitive inventory
 *
 * Shows all indexed primitives by kind, with optional filtering.
 */

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function registerPantryCommand(_pi: ExtensionAPI): void {
	// TODO: Implement pantry command
}
