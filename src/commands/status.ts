/**
 * /strudel status — overview dashboard
 *
 * Shows config, pantry counts, search mode, surface state at a glance.
 */

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function registerStatusCommand(_pi: ExtensionAPI): void {
	// TODO: Implement status command
}
