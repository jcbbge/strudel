/**
 * /strudel health — connectivity and configuration verification
 *
 * Checks that roots exist, embeddings endpoint responds, cache is accessible.
 */

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function registerHealthCommand(_pi: ExtensionAPI): void {
	// TODO: Implement health command
}
